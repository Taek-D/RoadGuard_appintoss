# RoadGuard MVP — Claude Progress Log

## Project Overview
출퇴근 경로 기상 위험 알림 앱인토스 미니앱
Seed: seed_886da26e8f95 | Ambiguity: 0.198

---

### Session 1 — 2026-04-09
- F001: 프로젝트 초기화 완료
  - granite.config.ts (displayName: "로드가드", primaryColor: #FF3B3B)
  - index.html viewport user-scalable=no, title/og:title "로드가드" 통일
  - SDK dynamic import 패턴 (src/lib/sdk.ts)
  - Firebase 초기화 (src/lib/firebase.ts)
  - Zustand GlobalStore (src/business/store/globalStore.ts)
  - AuthService, RouteManager, HazardEvaluator, ResiliencyController
  - functions/ 디렉토리 + package.json + tsconfig
  - 의존성: zustand, gsap, firebase 설치

- F002: 인증 + 인트로 화면 완료
  - IntroView: 앱 소개 + "시작하기" CTA → appLogin()
  - appLogin unlink callback → 로그아웃 처리
  - App.tsx 라우팅: authState 기반 조건부 렌더링
  - 웹 환경 mock userKey 자동 사용

- F003: 온보딩 (경로 설정) 완료
  - 3단계 폼: 출발지 → 도착지 → 알림 시간
  - Kakao Places API 주소 검색 + 350ms debounce
  - shadcn-ui Select 시간 피커 (05-12시, 10분 단위)
  - Firestore 저장 + NodeMatcherBatch 트리거

- F004: NodeMatcherBatch 완료
  - onCall Cloud Function (firebase-functions v2)
  - Kakao Directions → polyline decode → 1-2km 샘플링
  - Kakao 역지오코딩 → 행정구역 추출 + 중복 제거
  - ITS CCTV API → 경로 반경 CCTV 매핑
  - exponential backoff 재시도 (최대 3회)

- F005: WeatherCollectorBatch 완료
  - onSchedule 10분 cron
  - KMA 기상특보 API 파싱 (안개/대설/한파/결빙 등)
  - Firestore weatherAlerts 배치 업데이트
  - 이전 특보 해제 처리, API 장애 시 캐시 유지

- F006: PushNotifier 완료
  - onSchedule 1분 cron
  - KST 기준 commuteTime + 30분 매칭 (±2분 여유)
  - weatherAlerts 위험 판정 → 스마트 발송 API 호출
  - 중복 발송 방지 (notificationLog 당일 체크)

- F007: MapView 완료
  - Kakao Maps SDK 동적 로드 + 무채색 스타일
  - 경로 폴리라인 (안전: slate gray, 위험: neon red)
  - GSAP Pulse 위험 마커 애니메이션
  - HazardEvaluator 연동, ResiliencyController fallback

- F008: BottomSheetViewer 완료
  - vaul Drawer 기반 바텀시트 (60% 높이, 풀스크린 아님)
  - CCTV 스냅샷 이미지 로드 + 로딩/에러 fallback
  - 기상 정보 카드 (경보: 빨강, 주의보: 주황, 안전: 초록)
  - GSAP stagger 애니메이션

- F009: Mock 모드 전체 플로우 완료
  - MOCK_MODE 자동 감지 (VITE_KAKAO_JS_KEY 없으면 활성화)
  - MockMapView: CSS 기반 경로 시각화 (5개 CCTV 노드)
  - kakaoSearch mock: 강남/판교 검색 결과
  - OnboardingForm mock: Firestore/NodeMatcher 스킵
  - CCTV 스냅샷: picsum.photos mock 이미지

- Vercel 배포: https://roadguard-mvp.vercel.app

- 토스 심사 규칙 검증 통과:
  - ✅ alert/confirm/prompt 미사용
  - ✅ user-scalable=no
  - ✅ appLogin 사용자 액션 후 호출
  - ✅ 자체 header/nav 없음
  - ✅ 브랜딩 "로드가드" 통일
  - ✅ navigationBar withBackButton/withHomeButton

- 배포 후 버그픽스:
  - OnboardingForm: updateDoc → setDoc({merge:true}) + 5초 타임아웃 + mock fallback
  - MapView: SDK 로드 실패 시 MockMapView 자동 전환 (onSdkFail)
  - MapView: mock 기상특보 데이터 보존 (Firestore 빈 결과로 덮어쓰기 방지)
  - RoutePolyline: 'MOCK' polyline → MOCK_ROUTE_COORDS로 직접 그리기
  - BottomSheetViewer: max-h-[85vh] + X 닫기 버튼 추가

- API 키 발급 현황:
  - ✅ Kakao (JS + REST) — .env 입력 완료, 도메인 등록 완료
  - ✅ Firebase (Firestore) — holdem-ranking 프로젝트에 추가, .env 입력 완료
  - ✅ KMA 기상특보 — 공공데이터포털, .env 입력 완료
  - ⏳ ITS CCTV — 승인 대기 중

### Session 2 — 2026-04-10
- F010 진행 1: ITS CCTV API 연동 + 런타임 업그레이드
  - ✅ ITS API 키 발급 완료 → .env 및 Firebase Secrets 등록
  - ✅ ITS API 스펙 수정 (공식 문서 대조):
    - key → apiKey
    - ReqType: 2 제거, cctvType: 2 (스냅샷) 추가
    - MinX/MaxX/MinY/MaxY → 소문자
    - getType: 'json' 추가 (기본 XML 파싱 실패 방지)
  - ✅ Firebase Secrets 등록 (version 2):
    - ITS_API_KEY, KAKAO_REST_KEY, KMA_API_KEY
  - ✅ pushNotifier/weatherCollectorBatch에 secrets 선언 추가
  - ✅ Node 20 → 22 업그레이드 (firebase.json runtime + package.json engines)
  - ✅ firebase-functions ^5 → ^7.2.5 (Node 20 EOL 대응)
  - ✅ firebase-admin ^12 → ^13.8.0
  - ✅ Breaking changes 영향 없음 (이미 v2 API 사용 중)
  - ✅ Firebase Functions 재배포: nodeMatcherBatch, weatherCollectorBatch, pushNotifier (all Node.js 22, 2nd Gen)

- 다음 작업: F010 나머지
  - /harness-validate — NEVER/ALWAYS 규칙 위반 자동 탐지
  - /appintoss-nongame-launch-checklist 실행
  - Edge case 처리 (네트워크 단절, API 장애, 빈 경로)
  - 실 경로로 NodeMatcherBatch E2E 테스트 (Firestore routes.cctvNodes 확인)
  - 기능 스킴 URL 등록 + 랜딩 확인

### Session 3 — 2026-04-11
- F010 진행 2: /harness-validate 실행 + 고아 템플릿 파일 제거
  - harness-validate 1차 스캔 결과:
    - N1~N5 PASS (alert/header/auto-login/외부이동/설치유도 모두 깨끗)
    - N2 FAIL: src/pages/Brainstorm.tsx:270 <header>, src/pages/Index.tsx:404 hamburger
    - N6 PASS: src/lib/sdk.ts 완벽한 dynamic import + isSupported 패턴,
      authService.ts의 '@apps-in-toss/web-framework'는 callSdkApi 문자열 인자,
      granite.config.ts의 import type는 타입 전용 (런타임 erased)
    - A1~A4 PASS (viewport user-scalable=no, navigationBar, 브랜딩 "로드가드" 통일, IntroView)
    - C1~C3 N/A (광고/결제/공유 미구현)
  - 위반 원인 분석:
    - src/pages/Brainstorm.tsx, Index.tsx는 lovable.dev 스타터 템플릿 잔재
    - App.tsx는 HomeRouter(IntroView/OnboardingForm/MapView) + NotFound만 사용
    - 두 파일 어디에서도 import되지 않음 (완전 고아)
    - Vite tree-shaking으로 번들에서 빠지지만 git 소스에 남아 심사자 혼선 유발 + 실수 라우트 등록 리스크
  - 조치:
    - ✅ src/pages/Brainstorm.tsx 삭제
    - ✅ src/pages/Index.tsx 삭제
    - ✅ README.md: "src/pages/Index.tsx 수정" → "src/App.tsx 라우팅 + src/presentation/views/" 로 가이드 업데이트
    - ✅ npm run build 성공 (vite v6.4.1, 1923 modules, 7.17s)
    - ✅ harness-validate 재검증: NEVER 0건, ALWAYS 4/4, 모든 규칙 통과
  - 소프트 경고 (non-blocking):
    - src/presentation/views/MapView.tsx:562 window.location.reload()
      → 외부 이동 아닌 내부 리로드이므로 N4 규칙 위반 아님
      → UX 개선 관점에서 post-launch에 React state reset으로 교체 고려

- F010 진행 3: /appintoss-nongame-launch-checklist 1차 실행 + 반려 blocker 제거
  - 11단계 중 코드 범위 자체 검증 완료
    - 1단계 (접속/완결성): 코드 OK, 콘솔 의존 일부
    - 2단계 (네비바): ❌ 액세서리 설정 버튼 핸들러 미구현 → 반려 blocker
    - 3단계 (로그인): ✅ IntroView 사용자액션, unlink 콜백, 재로그인 플로우 OK
          (서버 JWT 교환 패턴은 실서비스 전 과제, 현재 mock 수준에서 토큰 노출 없음)
    - 4단계 (스킴/라우팅): 콘솔 의존
    - 5단계 (UI/UX): ✅ max-h-[85vh] + 닫기 X, overflow-hidden, 핀치줌 차단, toast/Drawer
    - 6단계 (브랜딩): ✅ "로드가드" 통일 (displayName, title, og:title, primaryColor #FF3B3B)
    - 7/8/11단계: N/A (결제/광고/공유 기능 없음)
    - 9단계 (외부 링크): ✅ 내부 완결, 설치 유도 0건
    - 10단계 (TDS): 미사용 (반려 사유 아님)
  - 반려 blocker 수정: 액세서리 버튼 제거
    - granite.config.ts: initialAccessoryButton 블록 완전 제거
    - 이유: 'settings' 버튼이 정의만 있고 클릭 핸들러/useNavigationBar 훅이 없어
      누르면 아무 동작 안 함 → "주요 기능 버튼 정상 동작" 규칙 위반
    - MVP 스펙상 설정 화면 불필요하므로 제거가 가장 안전
    - 필요 시 추후 useNavigationBar + SettingsView로 재도입 가능
  - 고아 asset 3개 정리 (Brainstorm/Index.tsx 삭제의 후속 작업):
    - ✅ src/assets/crimson-phantom.png 삭제
    - ✅ src/assets/hero-bg-shape.png 삭제
    - ✅ src/assets/logo.png 삭제 (118×113 — 콘솔 업로드 로고와 무관한 소스 자산)
    - src/assets 디렉토리 현재 비어있음, @/assets 참조 0건
  - 빌드 검증: vite 6.4.1 green (1923 modules, 3.00s)
    - 번들 크기 동일 (903KB) — 고아 asset은 원래도 tree-shaking으로 제외되어 있었음

- F010 진행 4: OnboardingForm chevron-left 버튼 → "이전" 텍스트 버튼 교체
  - 이유: 기존 chevron-left SVG (path d="m15 18-6-6 6-6")가 iOS/Android
    표준 네비게이션 백버튼 아이콘과 동일하여 심사자가 "자체 뒤로가기 UI"로
    오해할 가능성 있음 (NEVER 규칙: 자체 백버튼 금지)
    → 의미상은 폼 스텝 이동이지만 아이콘 형태만으로는 구분 불가
  - 수정: OnboardingForm.tsx:400~431
    - 원형 w-8 h-8 아이콘 버튼 → min-w-[3rem] 텍스트 "이전" 버튼
    - SVG path 완전 제거, 텍스트 라벨로 의도 명확화
    - aria-label="이전 단계" 유지 (접근성)
    - 양쪽 spacer min-w-[3rem]로 StepDots 중앙 정렬 유지
    - step 0일 땐 여전히 opacity-0 + pointer-events-none으로 감춰짐
  - 빌드 검증: vite 6.4.1 green (1923 modules, 3.08s)
    - 번들 903.58KB → 903.37KB (SVG 경로 제거로 소폭 감소)

- F010 진행 5: Edge case 처리 — 빈 데이터/설정 누락 시 UX 보강
  - 현재 커버리지 분석 결과, 이미 처리된 edge case가 9건 이상 있어
    (SDK 로드 실패 → MockMapView, MOCK_MODE 자동화, Firestore 5s 타임아웃,
    NodeMatcher 5s 타임아웃, route/hazards fetch fallback, CCTV 이미지 onError,
    empty districts 초기 리턴, home/work 미설정 → Seoul 중심, Weather API
    degraded banner 등) 실제 gap은 3건만 확인됨.
  - CLAUDE.md 원칙(right amount of complexity, no speculative abstractions)에
    따라 반려 리스크 있는 3건만 수정. 네트워크 오프라인 감지는 별도 컴포넌트
    필요하여 다음 세션 과제로 이월.
  - 수정 1: MapView.tsx fetchRoute null 가드 (반려 blocker)
    - 현상: Cloud Function이 아직 routeDoc 생성 전이거나 실패한 경우
      fetchRoute가 null 반환 → silently 지나가서 빈 지도만 표시 (경로선/CCTV 없음)
    - 위험: "앱 내 기능 완결성" 규칙 위반 가능성
    - 수정: fetchRoute 후 !routeData || routeData.cctvNodes.length === 0 체크
      → errorMessage "경로 정보를 아직 불러오지 못했습니다. 잠시 후 다시 시도하거나
         설정을 다시 확인해주세요." + loadingState='error' + early return
      → 기존 error overlay의 "다시 시도" 버튼으로 복구 경로 제공
    - Step 3 hazards 평가는 이미 non-null 보장된 이후로 이동 (불필요한 null 체크 제거)
  - 수정 2: kakaoSearch.ts REST_KEY 누락 시 mock fallback
    - 현상: MOCK_MODE는 VITE_KAKAO_JS_KEY만 체크. JS_KEY 있고 REST_KEY만 빠진
      상태에선 실 모드로 진입하지만 검색은 빈 배열만 반환 → 사용자는
      "검색 결과 없음"으로 오해
    - 수정: apiKey 없을 때 return [] → return mockSearch(query)
    - 효과: 부분 설정 환경(JS_KEY만 있는 로컬 dev 등)에서도 온보딩 진행 가능
  - 수정 3: CctvSnapshot.tsx 빈 URL 가드
    - 현상: ITS API가 특정 CCTV 노드에 대해 cctvurl 빈 값을 반환하면
      <img src=""> 로 브라우저 에러 유발
    - 수정: hasValidUrl 체크 (url && url.trim().length > 0) → 빈 URL이면
      status 초기값을 'error'로 설정하여 <img> 렌더 자체를 건너뛰고
      fallback UI 즉시 표시
    - 이 경우는 전체 CCTV API 장애가 아닌 특정 노드 데이터 결손이므로
      setApiStatus('cctv', 'degraded')는 호출하지 않음 (정상 케이스)
  - 빌드 검증: vite 6.4.1 green (1923 modules, 3.43s)
    - 번들 903.37KB → 903.61KB (가드 로직 240 바이트 증가)
    - TypeScript narrow 문제 없음 (early return으로 non-null 보장)

### Session 4 — 2026-04-11
- F010 진행 6: 네트워크 오프라인 감지 + 자동 복구
  - 신규 훅: src/business/hooks/useNetworkStatus.ts
    - navigator.onLine 초기값 + online/offline 이벤트 구독
    - SSR-safe (typeof navigator/window 가드)
    - mount 시 navigator.onLine 재동기화 (초기 state 시점 차이 보정)
  - MapView (RealMapView) 통합:
    - 상단 상태 바에 오프라인 배너 추가 (기존 degraded API 배너 위)
      - red-950/85 + border-red-500/40 + role="status" aria-live="polite"
      - "인터넷 연결이 끊어졌습니다" + 자동 복구 안내
      - 오프라인 중엔 degraded API 배너는 숨김 (노이즈 제거)
    - init useEffect 진입 시 offline 가드:
      - !navigator.onLine이면 errorMessage 설정 + loadingState='error' + early return
      - 불필요한 Kakao SDK/Cloud Function/Firestore 호출 억제
    - offline→online 전이 감지용 useEffect:
      - wasOfflineRef로 전이만 감지 (online 유지 시 no-op)
      - 전이 시 setRetryKey(k => k+1) → init useEffect 재실행
    - error overlay 조건부 UI:
      - isOnline: 기존 "다시 시도" 버튼 유지
        - BUT onClick을 window.location.reload() → setRetryKey(k => k+1) 로 교체
        - 이전 세션에서 선택 과제로 이월했던 "React state reset" 개선까지 동시 해결
        - 전체 페이지 리로드 없이 useEffect만 재실행 → selectedNode 등 기타 상태 보존
      - !isOnline: 버튼 숨기고 "연결 복구를 기다리는 중..." 안내 표시
        - 오프라인 중 reload 누르면 오히려 빈 페이지 로드로 악화되는 문제 회피
    - init useEffect deps: [user?.userId, retryKey] (retryKey 추가)
  - MockMapView는 미적용:
    - 네트워크 독립 (API 키 없어도 동작) → 오프라인 배너 불필요
    - 혼란만 유발할 수 있음
  - 빌드 검증: vite 6.4.1 green (1924 modules, 3.13s)
    - 모듈 수 1923 → 1924 (+1, useNetworkStatus 훅)
    - 번들 903.61KB → 905.02KB (+1.41KB, 배너 JSX + 훅 + 가드 로직)
  - harness 규칙 체크:
    - NEVER: alert/header/back/auto-login/외부이동/설치유도 추가 없음 ✅
    - ALWAYS: viewport/navigationBar/브랜딩/IntroView 변경 없음 ✅
    - SDK import 패턴 유지 ✅
    - navigator.onLine은 Web API이므로 SDK 패턴 대상 아님

### Session 5 — 2026-04-12~14
- F010 진행 7: 실 데이터 경로 복구 (7커밋) — MOCK에서 real API로 완전 전환
  - e354f47 fix(F010): load real CCTV data instead of leaving MOCK_ROUTE stuck
    - OnboardingForm: triggerNodeMatcher 후 fetchRoute(userId) + setRoute(realRoute)
      추가, 타임아웃 5s → 8s (Kakao Directions + reverse geocoding + ITS 여유)
    - MapView: route.polyline === 'MOCK'을 "real route 아직 없음"으로 취급,
      항상 Firestore 재조회. Firestore도 비어 있으면 MapView에서 nodeMatcher
      한 번 직접 트리거 → 업그레이드 시 mock weatherAlerts/hazardNodes 클리어
      → evaluateHazards 재실행 (실 행정구역 기준)
  - 42b632d fix(F010): soft-fallback map and widen CCTV query to highways
    - MapView: 빈 route/cctvNodes 시 error overlay 대신 MOCK fallback + degraded
      cctv 배너 (사용자는 항상 동작하는 지도를 봄)
    - nodeMatcher: overviewPolyline `A ?? B ? undefined : undefined` 버그 수정
      (?? 그룹 먼저 평가되어 항상 undefined 반환) → 직접 routes[0]?.overview_polyline
      읽기
    - nodeMatcher: CCTV 쿼리를 type: 'its'(국도)에서 'its' + 'ex'(고속도로)로 확장,
      cctvid 기준 dedup (좌표 fallback key). 경부/내부순환/분당내곡 경로 CCTV 복구
  - 502abcc fix(F010): bind Firestore to named database and switch nodeMatcher
    to onRequest — 3가지 프로덕션 버그 동시 해결
    - Root cause: holdem-ranking 프로젝트에 (default) Firestore가 없고 named DB
      "roadguard"만 존재. 클라이언트 getFirestore(app)와 functions admin.firestore()
      모두 (default)에 silent bind → 클라 timeout (MOCK fallback) / 서버 gRPC 5
      NOT_FOUND. 클라는 getFirestore(app, 'roadguard'), 3개 함수는 모두
      getFirestore('roadguard') from firebase-admin/firestore
    - onCall 403: Toss appLogin 사용하므로 Firebase ID token 없음. v2 onCall은 edge
      에서 인증 거부하고 `invoker` 옵션도 httpsTrigger(onRequest)에만 연결됨 →
      nodeMatcherBatch를 onRequest + cors:true + invoker:'public'으로 재작성,
      NodeMatcherError → HTTP status 매핑, 클라는 httpsCallable 대신 plain fetch
    - Region mismatch: 클라는 asia-northeast3 호출, 함수는 us-central1(v2 기본)
      배포 → us-central1 삭제 후 asia-northeast3 재배포
    - 검증: 존재하지 않는 userId로 호출 시 {"error":"not-found", ...} HTTP 404
      (reachable) → 이전엔 silent MOCK fallback이었음
  - 78def00 fix(F010): use initializeFirestore, trim env vars, add API timeouts
    - firebase.ts: getFirestore(app, dbId) → initializeFirestore(app, {}, dbId)
      (JS SDK가 DB path에 \n 삽입하던 encoding 버그 회피:
      projects/holdem-ranking\n/databases/roadguard). 모든 env var에 .trim()
      (Vercel/CI 공백 대응)
    - nodeMatcher: axios timeout 추가 (Kakao/ITS 15s, 역지오코딩 10s). 기존엔
      TCP 타임아웃 ~120s × retry × road type으로 300s deadline 초과 → 504
  - 57d8d98 fix(F010): switch KMA API to getPwnStatus for current weather warnings
    - 기존 getWthrWrnList (이력 조회)는 fromTmFc/toTmFc 필수 → 무조건 NO_DATA 반환,
      실 alert가 Firestore에 한 번도 기록되지 않았음 (앱은 영구 MOCK 상태)
    - getPwnStatus (특보현황조회)로 전환: 날짜 파라미터 없이 현재 active 특보 반환,
      파서가 이미 쓰던 t6/t7 필드명과 일치. "o 없음" sentinel 가드 추가하여
      "없음" 지역명 가짜 alert 생성 방지
  - db7b086 fix(F010): correct ITS cctvType to 3 (still image) and use HTTP
    - ITS 가이드 재확인: cctvType 2는 mp4 영상, 3이 스냅샷 (기존 주석 오류)
    - ITS API는 HTTP(80)에서만 응답, HTTPS(443)는 timeout → http:// 엔드포인트로 전환
    - 발급 키가 여전히 거부되는 문제 확인: 외부 이슈 (its.go.kr/opendata 재활성화
      필요), 코드 버그 아님
  - c0d99bd fix(F010): migrate ITS CCTV API to new endpoint
    - ITS 센터 확인: 기존 URL http://openapi.its.go.kr/api/NCCTVInfo는 구 OPEN API,
      곧 서비스 종료. 신규 발급 키는 신 엔드포인트만 동작 →
      https://openapi.its.go.kr:9443/cctvInfo로 교체
    - 기타: .claude/*.lock gitignore 추가

- 통합 효과: OnboardingForm → nodeMatcherBatch (asia-northeast3, onRequest, named DB)
  → Kakao Directions + 역지오코딩 + ITS CCTV (국도+고속도로) → Firestore
  routes.cctvNodes → MapView에서 real 데이터 렌더링. 실패/빈 결과 시에도 MOCK
  soft fallback + degraded 배너로 UX 유지.

### Session 6 — 2026-04-15
- F010 진행 8: Session 5 기록 + E2E 검증 스크립트 + exchangeAuthCode 엔드포인트
  - Session 5 7개 커밋 내용을 progress에 역추적 기록 (위 블록)

  - scripts/verify-e2e.mjs 신규 — NodeMatcherBatch E2E 검증 도구:
    - .env 로드 (dotenv 없이 직접 파싱) → Firebase Web SDK 초기화 → 'roadguard'
      named DB 바인딩
    - --seed=강남-판교 / 서울시청-성남 / 잠실-강남 프리셋으로 users/{id} 생성 가능
    - POST https://asia-northeast3-{projectId}.cloudfunctions.net/nodeMatcherBatch
      호출 후 HTTP 상태 + body + latency 로깅
    - routes/{userId} 재조회 → polyline/districts/cctvNodes 검증
    - Fail conditions: polyline empty / === 'MOCK' / districts 0 / cctvNodes 0
      → 각각 exit code 2~4로 구분
    - package.json에 `npm run verify:e2e` 스크립트 등록
    - node --check 구문 통과 확인
    - ITS API 키 활성화 후 실 경로로 회귀 테스트 가능한 상태
      (실행 명령: npm run verify:e2e -- --seed=강남-판교)

  - functions/src/exchangeAuthCode.ts 신규 — Toss OAuth → 앱 JWT 교환:
    - onRequest, asia-northeast3, cors:true, invoker:'public'
    - secrets: APP_JWT_SECRET (필수), TOSS_CLIENT_ID + TOSS_CLIENT_SECRET (선택)
    - Body: { authorizationCode, referrer? }
    - Toss mTLS 미연결 상태: TOSS_CLIENT_* 없으면 mock 모드로 degrade,
      authorizationCode sha256 앞 24자로 deterministic userKey 생성
      (실 서비스 전 console에 client cert 등록 후 https.Agent 연결 필요 — TODO)
    - referrer === 'UNLINK'|'WITHDRAWAL_*' 처리: Firestore users/{userKey}에
      unlinkedAt + unlinkReason 기록, 빈 token 반환으로 재로그인 유도
    - 정상 경로: users/{userKey} upsert (lastLoginAt + sessionNonce 8바이트) →
      HS256 JWT 발급 (node:crypto HMAC, 30일 TTL, payload: {sub, mode, iat, exp})
    - Toss OAuth 토큰은 서버 외부로 반환하지 않음 (CLAUDE.md "토큰 클라 노출 금지"
      규칙 준수)
    - functions/src/index.ts에 export 추가
    - functions/package.json: axios는 이미 의존성 존재 → 추가 dep 없이 구현

  - src/business/services/authService.ts 재작성 — 교환 플로우 통합:
    - LoginResult에 token 필드 추가 → { success, userKey, token }
    - 실 SDK 경로: appLogin() → { authorizationCode, referrer } 수신 →
      exchangeAuthCode POST → 서버 응답의 token + userKey 사용
    - 기존 authService는 `appLogin()`이 userKey를 직접 반환한다고 가정했으나
      SDK 2.x 실제 반환은 { authorizationCode, referrer } — 잠재 버그도 함께 수정
    - 웹 환경 (isWebEnvironment): 먼저 Cloud Function 호출 시도, 실패 시
      client-only mock(MOCK_USER_KEY)으로 degrade — 서버 미배포 dev에서도 동작
    - JWT_STORAGE_KEY ('roadguard:auth:token') localStorage 영속화 +
      getStoredToken / clearStoredToken 헬퍼 export (후속 API 호출에서 Authorization
      헤더로 활용 가능)
    - registerUnlinkCallback: SDK 콜백 래핑하여 unlink 시 clearStoredToken()
      자동 호출 후 기존 onUnlink 실행
    - exchange.unlinked === true 응답 받으면 success=false + token 비움 →
      IntroView는 자연스럽게 재로그인 화면으로 복귀

  - 빌드 검증:
    - functions: tsc (firebase-functions v7, Node 22) green
    - client: vite 6.4.1 build green (1924 modules, 899.11KB, gzip 267.32KB)
    - pre-existing 500KB+ chunk warning은 본 작업과 무관

  - 배포 전 필요 작업 (secrets 등록):
    - firebase functions:secrets:set APP_JWT_SECRET (랜덤 32바이트 권장)
    - firebase functions:secrets:set TOSS_CLIENT_ID (콘솔에서 발급)
    - firebase functions:secrets:set TOSS_CLIENT_SECRET (콘솔에서 발급)
    - TOSS_CLIENT_* 미등록 상태에서는 mock 모드로 동작하므로 배포 자체는 가능

- F010 진행 9: 배포 + E2E 실행 + GCP egress 이슈 발견
  - 배포:
    - APP_JWT_SECRET 신규 생성(48바이트 base64url) → Secret Manager version 1 등록
    - exchangeAuthCode.ts secrets 배열에서 TOSS_CLIENT_ID/SECRET 제거
      (Firebase는 배열 전원이 존재해야 deploy 허용, mTLS 연결 전까지는 APP_JWT_SECRET만)
    - firebase deploy --only functions:exchangeAuthCode →
      https://asia-northeast3-holdem-ranking.cloudfunctions.net/exchangeAuthCode
    - Smoke test: POST {authorizationCode:"smoke-test-abc123", referrer:"SANDBOX"}
      → HTTP 200, HS256 JWT 발급, expiresIn: 2592000(30일),
      userKey: mock_93b9f05b52ce211b98e3fdd0 (deterministic sha256)
    - 커밋: 649bf46 (secrets array fix)

  - E2E 1차 실행 (강남-판교 프리셋):
    - HTTP 200, 95초 소요 (Kakao 경로 탐색 + reverse geocode + ITS ×2 retry)
    - districts: 4개 정상 추출 ("서울특별시 서초구/강남구", "경기도 성남시 수정구/분당구")
    - polyline: len=6148 (vertex 기반 재구성 정상)
    - cctvNodes: 0 ❌
    - 함수 로그 분석: AxiosError timeout 15000ms ECONNABORTED 반복 발생
    - 로컬 curl 검증: 동일 URL이 HTTP 200 + 59 CCTVs 즉시 응답
    - 결론: ITS API는 정상이나 Cloud Functions(asia-northeast3) egress IP에서
      openapi.its.go.kr:9443로의 TCP/TLS 연결이 매우 느림 (15s 타임아웃)
    - 조치: axios timeout 15s → 30s 상향 후 재배포 (커밋 340fb9d)

  - E2E 2차 실행 (30s timeout):
    - HTTP 200, 184초 소요 (30s × 3 retries × 2 road types + Kakao)
    - 여전히 cctvNodes: 0 ❌
    - 30초로도 부족 — egress IP가 ITS 측에서 slow-start 또는 rate-limit 대상
    - 단순 timeout 증가로는 해결 불가, 인프라 레벨 조치 필요

  - 근본 원인 진단:
    - 공공기관 API(국토부 ITS)는 통상 특정 IP만 신뢰하는 네트워크 정책 운영
    - GCP Cloud Functions 2nd Gen은 dynamic egress IP 사용 → cold IP로 판별됨
    - 로컬 curl은 ISP의 한국 고정 IP로 나가므로 정상 응답
    - 해결: VPC Connector + Cloud NAT static IP → ITS 콘솔에 IP 화이트리스트 등록
      (별도 인프라 작업, 이번 세션 범위 밖)

  - 현재 방어 상태:
    - MapView.tsx는 route.cctvNodes.length === 0이면 MockMapView로 soft fallback
    - + degraded CCTV 배너로 사용자 안내
    - 기상 알림(KMA)과 경로 표시는 real 데이터로 동작, CCTV만 mock
    - 심사 관점: 앱 작동 중단 없음, UI 완결성 유지

- F010 진행 10: ITS CCTV 완전 제거 → 기상특보 전용 MVP
  - Vercel icn1 probe 결과: ITS 서버가 Vercel egress도 10초 만에 fetch failed
    (GCP asia-northeast3와 동일한 차단 패턴)
  - 결론: 클라우드 서버리스 egress IP 전반이 ITS의 IP allow list에서 누락.
    근본 해결은 static egress IP + ITS 콘솔 등록 필요 → 이번 릴리즈 범위 밖
  - 제품 가치는 "경로상 기상 위험 알림"이고 CCTV는 보조 시각화였으므로
    CCTV 섹션 전체 제거하고 district 단위 기상특보만으로 MVP 출시
  - 변경:
    - functions/src/nodeMatcherBatch.ts:
      - secrets 배열에서 ITS_API_KEY 제거 (KAKAO_REST_KEY만)
      - ITS fetch 블록 80여 줄 완전 제거 (bbox, 2 road types, retries, 30s timeout)
      - cctvNodes = [] 상수 대입, routeDoc 스키마는 그대로 유지
      - Kakao Directions + reverse geocode는 그대로 (districts, polyline real)
    - src/presentation/views/MapView.tsx:
      - `cctvNodes.length === 0 → MOCK fallback` 가드 제거
        (기존엔 real 경로도 cctv 0개면 MOCK으로 되돌려 버렸음)
      - 이제 routeData 자체가 null인 경우(cold start, CF 실패)에만 MOCK fallback
      - setApiStatus('cctv', 'degraded') 호출도 함께 삭제 (이제 정상 경로)
    - scripts/verify-e2e.mjs:
      - cctvNodes.length === 0 FAIL 조건 제거, PASS 메시지 update
    - api/cctv-probe.ts:
      - 진단용 Vercel Function이었으므로 삭제
    - vercel.json:
      - /api/* rewrite는 유지 (향후 다른 serverless function에 재사용 가능)
  - Firebase 재배포: functions:nodeMatcherBatch → Successful update
  - E2E 결과:
    - HTTP 200, 718ms (이전 184000ms → 255배 빠름)
    - districts: 4개 (서초/강남/수정/분당) real 데이터
    - polyline: 6148자 (vertex 재구성, real)
    - cctvNodes: 0 (의도된 동작)
    - ✅ PASS
  - 커밋: ff7f0b1 (6 files changed, 47 insertions, 277 deletions — 순삭감)

- F010 진행 11: CCTV 완전 제거 (3 Phase, 영구 결정)
  - 결정 배경: ITS는 한국 공공기관 전용 API이고 클라우드 egress IP를
    차단함이 확인되어, 재도입 경로가 없다고 판단 → dead branch 유지 대신
    순제거로 기술 부채 청산
  - Phase 1 (3b26f22): RealMapView CCTV UI 제거, HazardMarker.tsx 삭제
    - -229 LoC, 빌드 895KB (Phase 0 대비 -4KB)
    - selectedNode / isSheetOpen / handleMarkerClick / selectedNodeAlert /
      HazardMarker 렌더 / "CCTV 보기" 버튼 / BottomSheetViewer 마운트 전부 제거
    - RealMapView는 map + polyline + weather banner + loading/error 만 남음
  - Phase 2 (ea3ded2): MockMapView district 기반 재작성
    - -335 LoC, 빌드 779KB (-115KB, gzip 267 → 225KB, -42KB)
    - CCTV 노드 리스트 → route.districts 리스트 + 구역별 기상특보 카드
    - BottomSheetViewer.tsx, CctvSnapshot.tsx 삭제
    - mockData.ts: MOCK_ROUTE.cctvNodes = [], MOCK_HAZARD_NODES 제거
    - OnboardingForm.tsx: setHazardNodes / MOCK_HAZARD_NODES / cctvNodes>0
      가드 제거
    - 번들 급감 이유: vaul Drawer (BottomSheetViewer의 유일한 소비자) 제거
  - Phase 3 (779e475): 타입/서비스 레이어 순제거
    - -77 LoC
    - globalStore: CctvNode 인터페이스, RouteData.cctvNodes, hazardNodes
      state, setHazardNodes action 전부 삭제
    - hazardEvaluator: HazardResult.hazardCctvNodes 제거, 시그니처를
      evaluateHazards(districts) → { isHazardous, alerts }로 단순화.
      node.name 기반 구역명 substring 매칭 로직 삭제 (실 데이터에서 항상
      부정확했던 근사치)
    - resiliencyController: ApiStatus.cctv 키 + fallbackMessage 제거.
      weather 메시지에서 "CCTV 정보만 제공" 문구 제거
    - routeManager.fetchRoute: cctvNodes 필드 매핑 제거
    - nodeMatcherBatch: CctvNode 인터페이스, RouteDoc.cctvNodes 필드 제거.
      응답 body에서 cctvCount 제거. Firestore 기존 doc의 stale cctvNodes는
      merge:true로 그대로 남지만 어디서도 읽지 않음
  - Firebase 재배포: functions:nodeMatcherBatch (Phase 3) successful update
  - E2E 재검증: HTTP 200 in 1.6s, districts 4개 real, ✅ PASS
  - 총 순제거: -641 LoC (329 → 클라이언트 전용 시나리오 전부 심플화)

- 향후 남은 작업 (우선순위 순):
  - 앱인토스 콘솔: 기능 스킴 URL 등록 + 랜딩 렌더링 확인 (콘솔 + 실기기)
  - 앱인토스 콘솔: 로고 600×600 각진 정사각형 PNG 업로드 (별도 준비 필요)
  - 실기기: 최초 화면 백버튼 누르면 미니앱 종료되는지 확인
  - 실서비스 전: exchangeAuthCode에 Toss mTLS client cert 연결 (https.Agent + cert/key)
    → 연결 시 secrets 배열에 TOSS_CLIENT_ID/SECRET 재추가
  - Firebase/Vercel의 ITS_API_KEY secret은 비활성 상태로 남아있음 — 정리 원하면 제거 가능
